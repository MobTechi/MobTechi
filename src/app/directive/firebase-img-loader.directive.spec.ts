import { FirebaseImgLoaderDirective } from './firebase-img-loader.directive';

describe('FirebaseImgLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new FirebaseImgLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
